{
    "name": "vision-electoral",
    "private": true,
    "packageManager": "pnpm@10.33.4",
    "engines": {
      "node": ">=22.0.0",
      "pnpm": ">=10.0.0"
    },
    "scripts": {
      "dev:web": "pnpm --filter web dev",
      "dev:api": "pnpm --filter api dev",
      "build:web": "pnpm --filter web build",
      "build:api": "pnpm --filter api build",
      "lint": "pnpm -r lint",
      "test": "pnpm -r test"
    },
    "pnpm": {
      "onlyBuiltDependencies": ["esbuild", "firebase-tools"]
    }
  }